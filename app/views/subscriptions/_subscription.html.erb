<span class = 'headline'>
<% div_for(subscription) do %>

<%= link_to subscription.author.name, :controller => 'authors', :action => 'show', :id => subscription.author, :l => @l %>&nbsp;&nbsp;
<%= link_to image_tag('minus.gif'), :controller => 'subscriptions', :action => 'remove', :id => subscription.id, :l => @l %>
<br><br>
<% end %>
</span>