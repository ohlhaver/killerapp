<b><%= link_to recommendation.recommender.fb_user.name, {:controller => 'users', :action => 'profile', :id => recommendation.recommender.id} -%></b> <%= '<b> ' if recommendation.active %> recommended you <%= recommendation.article? ?  'article' : 'author' %> <%= '</b> ' if recommendation.active %> <%= render :partial => recommendation.resource %>
