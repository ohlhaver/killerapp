<%= stylesheet_link_tag 'style' %>

<div id= "filter">
<b><%= 'alle Artikel - ' if @current_user.filter != 1 %></b> 
<%= (link_to 'Meinungen zeigen',  {:controller => 'rawstories', :action => 'filter_by_opinions'}) if @current_user.filter != 1 %>

<%= link_to 'alle Artikel zeigen',  {:controller => 'rawstories', :action => 'show_all'} if @current_user.filter == 1 %>
<b><%= ' - Meinungen'  if @current_user.filter == 1 %></b>
</div>


<p>

Suchergebnisse für <b>'<%= @current_user.query %>'</b>
     
<% unless @rawstories.empty? -%> 

</p>
<p>

    <%= render :partial => @rawstories %>
</p>
<div id= "pagination" > 
<%= will_paginate(@rawstories) %>
</div>
<br>
<% else -%>
  <p><%= 'Keine Artikel gefunden.' if @current_user.filter == 0 %>
<%= 'Keine <b>Meinungen</b> gefunden.' if @current_user.filter == 1 %>
</p>
<% end -%>






