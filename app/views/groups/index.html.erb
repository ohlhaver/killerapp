

<% cache ({:page => params[:page] || 1}) do %>


<ul>



<%= render :partial => @haufens %>

</ul>

<div class= "pagination" > 
<%= will_paginate(@haufens, options = {:prev_label => 'zurück', :next_label => 'vorwärts'}) %>
</div>

<%end%>

